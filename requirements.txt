ultralytics
mss
numpy
fonttools
requests
Pillow
pyyaml
tqdm
scipy
matplotlib
torch
torchvision
pandas
# Local com GPU NVIDIA: o pip acima instala a build CPU-only por padrao no
# Windows. Pra usar a GPU local em inferencia/avaliacao (Kaggle ja vem com
# CUDA pronto, isso e' so pra rodar local), reinstalar com o indice certo:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu130 --force-reinstall --no-deps
# (troque cu130 pela build disponivel pra sua versao -- ver download.pytorch.org/whl/torch/)
jaconv
bitstring
pytest
easyocr
pytesseract
manga-ocr
https://github.com/CaptainDario/ETLCDB_data_reader/releases/download/v2.1.4/etl_data_reader_CaptainDario-2.1.4-py3-none-any.whl
# opencv-python tem que ser instalado POR ULTIMO: easyocr traz opencv-python-headless
# como dependencia, e os dois pacotes escrevem no mesmo modulo cv2 -- quem instala por
# ultimo "vence". headless nao tem suporte a GUI (cv2.imshow falha), entao precisamos
# que o opencv-python (com GUI) reinstale por cima dele.
opencv-python
